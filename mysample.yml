--- 
- hosts: all
  become : yes
  tasks :
        - name : install tree
          apt :
            name : tree
            state : present

        - name : install wget
          apt :
            name : wget
            state : present


        - name : install tree
          yum :
             name : tree
             state : present


        - name : install wget
          yum : 
             name : wget
             state : present       